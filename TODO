TODO
====

Consul integration - Read config settings. Upload new settings. Register as a
service. Ensure that healthcheck mechanism is properly configured/implemented.

Readings - Assertions and (post-Delhi) mappings. Binary encodings.
base64 encoding of floating point numbers.

Discovery endpoint - Only add new devices which match a provisionwatcher.
These to be dynamically created according to configuration.

Scheduling - Get ScheduleEvents marked for our service from metadata. Upload
ScheduleEvents and Schedules in our configuration that aren't in metadata.

Threadpool - Configure number of threads.

Handle resource operations which link to profile resource rather than device
resource.
